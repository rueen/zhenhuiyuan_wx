<view class="records-page">
  <view class="records-list">
    <view class="record-item card" wx:for="{{records}}" wx:key="id">
      <view class="record-left">
        <text class="record-amount price"><text class="price-symbol">¥</text>{{item.amount}}</text>
        <text class="record-time">{{item.created_at}}</text>
        <text wx:if="{{item.reject_reason}}" class="record-reason">{{item.reject_reason}}</text>
      </view>
      <view class="record-meta">
        <text class="record-status status-wd-{{item.status}}">{{item.statusText}}</text>
        <text class="record-status-text" wx:if="{{item.reject_reason}}">已返回至余额</text>
        <text class="record-status-text" wx:else>{{item.account_snapshot.account_no_mask}}</text>
      </view>
    </view>

    <empty wx:if="{{!loading && records.length === 0}}" text="暂无提现记录" />

    <view class="load-more" wx:if="{{loading}}">加载中…</view>
    <view class="load-more" wx:if="{{!loading && noMore && records.length > 0}}">没有更多了</view>
  </view>
</view>
