<view
  class="nav-back"
  style="left:{{left}}px;top:{{top}}px;width:{{size}}px;height:{{size}}px;"
  bindtap="onTap"
>
  <icon name="{{hasBack ? 'arrow-l' : 'home'}}" size="20" color="#000" />
</view>
